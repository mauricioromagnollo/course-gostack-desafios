
// funciona da mesma forma que com nome
export default function (base: number, altura: number ): number {
  return base * altura
}

/* Export com nome:
export default function areaRetangulo(base: number, altura: number ): number {
  return base * altura
}
*/
